# RunPod has internet ON, so we install from PyPI here (unlike Kaggle's offline path).
# torch normally ships in the RunPod "PyTorch" pod template; listed for completeness.
repeng>=0.5.0
transformers>=4.36.2
matplotlib>=3.7
accelerate>=0.26        # for device_map / larger loads
